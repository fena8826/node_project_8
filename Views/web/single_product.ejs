
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= product.title %> | Product Detail</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="
    https://cdn.jsdelivr.net/npm/sweetalert2@11.17.2/dist/sweetalert2.all.min.js
    "></script>
  <style>
    body {
      background-color: #f5f5f5;
      font-family: 'Segoe UI', sans-serif;
    }

    .product-card {
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
      background-color: #fff;
    }

    .product-img {
      height: 100%;
      object-fit: contain;
    }

    .product-details {
      padding: 30px;
    }

    .product-title {
      font-size: 28px;
      font-weight: 600;
      margin-bottom: 10px;
    }

    .product-desc {
      font-size: 15px;
      color: #555;
    }

    .product-meta strong {
      color: #111;
    }

    .product-price {
      font-size: 24px;
      color: #e91e63;
      font-weight: bold;
    }

    .btn-myntra {
      background-color: #ff3f6c;
      color: #fff;
      font-weight: bold;
    }

    .btn-myntra:hover {
      background-color: #e91e63;
    }

    .back-btn {
      margin-left: 10px;
      background-color: #ff3f6c;
      border: none;
    }

    .label {
      font-size: 14px;
      color: #999;
      margin-bottom: 2px;
    }
  </style>
</head>
<body>

  <div class="container mt-5 mb-5">
    <div class="row justify-content-center">
      <div class="col-lg-10">
        <div class="row g-0 product-card">
          
          <!-- Product Image -->
          <div class="col-md-5">
            <img src="<%= product.productImage %>" alt="<%= product.title %>" class=" img-fluid product-img w-100">
          </div>

          <!-- Product Info -->
          <div class="col-md-7">
            <div class="product-details">
              <h1 class="product-title"><%= product.title %></h1>
              <p class="product-desc mb-3"><%= product.desc %></p>             
              <div class="product-meta mb-3">
                <div class="label">CATEGORY</div>
                <div><strong><%= product.category?.category || 'N/A' %></strong></div>                
              </div>
              <div class="mb-4">
                <div class="label">PRICE</div>
                <div class="product-price">₹ <%= product.price %></div>

                <div class="label mt-3">AVAILABLE QUANTITY</div>
                <div><%= product.quantity %> units</div>
              </div>

              <div class="d-flex flex-wrap gap-3">
                
                <a href="/web/product" class="btn btn-secondary back-btn">Back to List</a>
              </div>

            </div>
          </div>

        </div>
      </div>
    </div>
  </div>
</body>
</html>